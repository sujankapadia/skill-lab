# Contributing

- Run `python -m pytest` before opening a pull request.
- Architecture documentation lives in `docs/`. `docs/architecture.md` is the
  canonical overview; significant design decisions get an ADR in `docs/adr/`.
  Keep these current when you change the structure of the code.
- Do not commit `inventory.json` or `inventory.db`.
